{
  "name": "mcp-powershell-exec",
  "version": "1.0.0",
  "description": "PowerShell MCP server with improved architecture using Bun and TypeScript",
  "main": "powershell.ts",
  "type": "module",
  "scripts": {
    "start": "bun run powershell.ts",
    "dev": "bun --watch powershell.ts",
    "build": "bun build powershell.ts --outdir dist --target bun",
    "lint": "tsc --noEmit"
  },
  "keywords": ["mcp", "powershell", "bun", "typescript"],
  "author": "cuipengfei",
  "license": "MIT",
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.18.0"
  },
  "engines": {
    "bun": ">=1.0.0"
  }
}
